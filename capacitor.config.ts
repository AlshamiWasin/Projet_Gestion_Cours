import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Project_Gestion_Cours',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
